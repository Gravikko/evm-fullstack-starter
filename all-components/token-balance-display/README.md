# Token Balance Display

Show ETH and ERC20 token balances for connected wallet.

## What it does
- Real-time balance updates
- Formatted display (no raw wei)
- Multiple token support
- Loading states

## Usage
Hook: `useTokenBalance()` returns `{ address, balance, symbol, isLoading }`

No smart contract needed.
